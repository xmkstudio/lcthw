// new style
const printPerson = (name, age) => {
  console.log(`Hi ${name}, you are ${age} years old.`);
};

// other (old?) style
function printPerson(name, age) {
  console.log(`Hi ${named}, you are ${age} years old.`);
}
